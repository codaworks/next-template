model User {
    id      Int      @id @default(autoincrement())
    email   String   @unique
    name    String?
    created DateTime @default(now())
    updated DateTime @updatedAt

    @@map("account_user")
}
